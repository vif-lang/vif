/// Runtime of Vif.
/// This module is the first one being analyzed per compîlation unit.

const root = @import("root");

// The actual main function called by the OS.
// Signature must be synchronized with the compiler and tests.
fn main(argc: i32, argv: *const *const u8) i32 {
    root.main();
    return 0;
}
