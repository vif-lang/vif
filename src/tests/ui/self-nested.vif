const Outer = struct {
    const Inner = struct {
        value: i32,

        fn new() Self {
            return .{ .value = 0 };
        }
    };
};

fn main() void {
    var val = Outer.Inner.new();
}

//@ args: build
