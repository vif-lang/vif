const std = @import("std")
extern "c" fn printf(fmt: *const u8, ...) i32

fn main() void {
    var i: i32 = 0
    var sum: i32 = 0
    while i < 5 {
        sum += i
        i += 1
    }
    printf(std.slice.ptr("%d\n"), sum)
}

//@ args: run
//@ stdout:
// 10
